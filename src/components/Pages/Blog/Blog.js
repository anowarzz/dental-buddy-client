import React from 'react';
import useTitle from '../../../Hooks/useTitle';

const Blog = () => {
useTitle('Blog')


    return (
        <div>
            <h3>This is the blog section</h3>
        </div>
    );
};

export default Blog;